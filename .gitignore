.trace/runtime/

# Python bytecode (the bench/cib island)
__pycache__/
*.pyc

# Locally-generated cross-CLI mirrors (not canonical; regenerated by local tooling).
# Canonical sources are skills/<name>/SKILL.md and agents/<name>.md.
.agents/
.codex/
